{
    "name": "mf/collections-php",
    "description": "Collections for PHP - It's basically a syntax sugar over classic array structure, which allows you to use it as classic array, but adds some cool features.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Petr Chromec",
            "email": "mortal_flesh@seznam.cz"
        }
    ],
    "autoload": {
        "psr-4": {
            "MF\\Collection\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "MF\\Collection\\": "tests/"
        }
    },
    "require": {
        "php": "^8.0",
        "ext-mbstring": "*",
        "beberlei/assert": "^3.0",
        "mf/type-validator": "^4.1"
    },
    "require-dev": {
        "lmc/coding-standard": "^3.2",
        "mockery/mockery": "^1.4",
        "phpstan/extension-installer": "^1.1",
        "phpstan/phpstan": "^1.3",
        "phpstan/phpstan-beberlei-assert": "^1.0",
        "phpstan/phpstan-mockery": "^1.0",
        "phpstan/phpstan-phpunit": "^1.0",
        "phpunit/phpunit": "^9.5",
        "symfony/var-dumper": "^6.0"
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "composer/package-versions-deprecated": true,
            "dealerdirect/phpcodesniffer-composer-installer": true,
            "phpstan/extension-installer": true
        }
    },
    "scripts": {
        "all": [
            "@analyze",
            "@tests-ci"
        ],
        "analyze": [
            "@cs",
            "@phpstan"
        ],
        "cs": "vendor/bin/ecs check --ansi -- src/ tests/",
        "phpstan": "vendor/bin/phpstan analyze src tests -c phpstan.neon --ansi --level 8 --memory-limit=-1",
        "tests": "vendor/bin/phpunit",
        "tests-ci": "php -dxdebug.coverage_enable=1 vendor/bin/phpunit -c phpunit.xml.dist --exclude-group local",
        "fix": [
            "vendor/bin/ecs check --ansi --clear-cache --fix -- src/ tests/"
        ]
    }
}
